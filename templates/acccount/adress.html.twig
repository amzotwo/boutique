{% extends 'base.html.twig' %}

{% block title %}Mes adresses!{% endblock %}

{% block content %}
    <a href="{{ path('app_acccount_adress_add') }}" class="btn btn-info btn-sm float-right">Ajouter une adresse</a>
    <h1>Mes adresses</h1>
    C'est dans cet espace que vous aller gerer toutes vos adresses
    <hr>
    <a href="{{ path('app_acccount') }}">  - Retour </a><br/>
    <hr>

    {% if app.user.adresses|length == 0 %}
        <p class="text-center">
        Vous n'avez pas encore ajouté d'adresse dans votre compte client. Pour en ajouter une veuillez <a href="{{ path('app_acccount_adress_add') }}">cliquer ici</a>
        </p>
    {% else %}

            <div class="row">
                {% for adress in app.user.adresses %}
                <div class="col-md-4 mb-2">
                    <div class="card" style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title">{{ adress.name }}</h5>
                            <p class="card-text">
                                {{ adress.adress }} <br/>
                                {{ adress.postal }} - {{ adress.city }} - {{ adress.conutry }}
                            </p>
                            <a href="{{ path('app_acccount_adress_edit',{ 'id':adress.id}) }}">Modifier</a> |  <a href="{{ path('app_acccount_adress_delete',{ 'id':adress.id}) }}">Supprimer</a>
                        </div>
                    </div>
                </div>
        {% endfor  %}
            </div>



    {% endif  %}
{% endblock %}
